'use strict';

// const waitFor = (ms) => new Promise(r => setTimeout(r, ms))
// [1, 2, 3].forEach(async (num) => {
//   await waitFor(50)
//   console.log(num)
// })
// console.log('Done')

// function each(arr, cb) {
//   for (let index = 0; index < arr.length; index++) {
//     cb(null, arr[index], index);
//   }
// }

// each([1, 2, 3, 4, 5, 6], (err, value, i) => {
//   console.log(value, i);
// });

